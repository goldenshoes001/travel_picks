import 'package:flutter/material.dart';

class Abstandshalter extends StatelessWidget {
  const Abstandshalter({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [const SizedBox(height: 24), const SizedBox(height: 8)],
    );
  }
}
